IT378 Homework 4
Write the complete table create statements for the tables in the model below:
-----------------------------------------
Example: https://www.w3schools.com/sql/sql_primarykey.asp

CREATE TABLE Persons (
    ID int NOT NULL,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255),
    Age int,
    CONSTRAINT PK_Person PRIMARY KEY (ID,LastName)
);
-----------------------------------------

CREATE TABLE department 
(
Dept# INT NOT NULL,
Dept_name varchar(20),
Phone INT,
CONSTRAINT Dept_ID PRIMARY KEY(Dept#)
);

CREATE TABLE major
(
Major# INT NOT NULL,
Major_name varchar(20),
CONSTRAINT Major_PK PRIMARY KEY(Major#)
);

CREATE TABLE student
(
Student# INT NOT NULL,
Student_name varchar(20),
CONSTRAINT Student_PK PRIMARY KEY(Student#)
);


CREATE TABLE section
(
Section# INT NOT NULL,
Semester INT,
DateTime Date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
CONSTRAINT Section_PK PRIMARY KEY(section#)
);

CREATE TABLE instructor
(
inst_id INT NOT NULL,
inst_name varchar(20),
CONSTRAINT inst_PK PRIMARY KEY(inst_id)
);
